import { FormContainer, LevelTitle } from "./components/common";

const Fallback = () => {
  return (
    <FormContainer>
      <LevelTitle text={"出错啦！"} />
    </FormContainer>
  );
};

export default Fallback;
